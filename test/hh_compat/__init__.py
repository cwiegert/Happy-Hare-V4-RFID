# test/hh_compat/__init__.py
#
# Compatibility harness: verifies this repo's klippy/extras/nfc_gates
# package (specifically the shared-reader path) against a real, local
# Happy Hare checkout's klippy/extras/mmu package -- real source, not
# hand-maintained duplicates of its API shape.
